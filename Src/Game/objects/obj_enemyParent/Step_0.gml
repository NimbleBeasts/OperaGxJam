direction = point_direction(x, y, global.player.x, global.player.y); // move to player's direction


if (has_been_hit_this_step) {
	has_been_hit_this_step = false;
	damage_received = 0;
	//reduce hitpoints
}